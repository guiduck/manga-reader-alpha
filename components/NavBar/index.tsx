import { Flex, Menu, MenuItem, Button } from '@chakra-ui/react';
import React from 'react';

const NavBar: React.FC = () => {
  return (
    <Flex>
      <Menu>
        <MenuItem>
        </MenuItem>
      </Menu>
    </Flex>
  );
}

export default NavBar;